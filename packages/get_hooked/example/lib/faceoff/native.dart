import 'package:flutter/material.dart';

class NativeFaceoff extends StatelessWidget {
  const NativeFaceoff({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
