import 'package:flutter/material.dart';
import 'package:get_hooked/get_hooked.dart';

final getBools = Get.list(const <bool>[]);

class GetHookedFaceoff extends StatefulWidget {
  const GetHookedFaceoff({super.key});

  @override
  State<GetHookedFaceoff> createState() => _GetHookedFaceoffState();
}

class _GetHookedFaceoffState extends State<GetHookedFaceoff> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}

class GetHookedWrapper extends StatelessWidget {
  const GetHookedWrapper({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
