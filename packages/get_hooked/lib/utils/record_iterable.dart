/// ## Disclaimer
/// Not currently used at all; prone to removal/relocation.
library;

// ignore_for_file: public_member_api_docs, not gonna do that, sorry :(

extension RecordIterable2<T> on (T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
  }
}

extension RecordIterable3<T> on (T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
  }
}

extension RecordIterable4<T> on (T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
  }
}

extension RecordIterable5<T> on (T, T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
    yield $5;
  }
}

extension RecordIterable6<T> on (T, T, T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
    yield $5;
    yield $6;
  }
}

extension RecordIterable7<T> on (T, T, T, T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
    yield $5;
    yield $6;
    yield $7;
  }
}

extension RecordIterable8<T> on (T, T, T, T, T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
    yield $5;
    yield $6;
    yield $7;
    yield $8;
  }
}

extension RecordIterable9<T> on (T, T, T, T, T, T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
    yield $5;
    yield $6;
    yield $7;
    yield $8;
    yield $9;
  }
}

extension RecordIterable10<T> on (T, T, T, T, T, T, T, T, T, T) {
  Iterable<T> toIterable() sync* {
    yield $1;
    yield $2;
    yield $3;
    yield $4;
    yield $5;
    yield $6;
    yield $7;
    yield $8;
    yield $9;
    yield $10;
  }
}
